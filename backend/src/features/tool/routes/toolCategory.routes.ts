// import { Router } from 'express';
// import { ToolCategoryController } from '../controllers/toolCategory.controller.ts';
// import { authMiddleware } from '../middleware/auth.middleware.ts';

// const controller = new ToolCategoryController();
// const router = Router();

// router.post('/', authMiddleware, controller.create.bind(controller));
// router.get('/', authMiddleware, controller.getAll.bind(controller));
// router.get('/:tool_id/:category_id', authMiddleware, controller.get.bind(controller));
// router.put('/:tool_id/:category_id', authMiddleware, controller.update.bind(controller));
// router.delete('/:tool_id/:category_id', authMiddleware, controller.delete.bind(controller));

// export default router;
